# BOT_BUTTONS_UX_ISSUES

## 1. Обзор

Этот документ фиксирует нюансы работы кнопок и UX‑поведение бота по всем основным сценариям:

- Глобальные кнопки: «Главное меню», «Назад», «Отменить», команды `/start`, `/q`.
- Кнопки сценариев: проверка (check), добавление (add), редактирование (edit), теги (tag), пересылка, фото/документы.
- Поведение при нажатии кнопок из старых/неактуальных сообщений и при пересечении сценариев.

На основе `BOT_SCENARIOS_ANALYSIS.md` большинство сценариев помечены как ✅, поэтому здесь перечислены только реальные или потенциальные UX‑проблемы.

---

## 2. Глобальные кнопки и команды

### 2.1. «🏠 Главное меню» / `/start`

**Контекст:**  
Практически во всех сценариях используются inline‑кнопки с `callback_data="main_menu"`, а также команда `/start`.

**Фактическое поведение:**

- `main_menu`:
  - Через `button_callback` всегда приводит пользователя к главному меню и очищает связанные с previous‑screen ID сообщения из списков очистки.
  - Используется как «универсальный выход» из большинства экранов.
- `/start`:
  - Во всех `ConversationHandler` зарегистрирована как fallback.
  - Очищает состояние и показывает главное меню.

**Проблема:**  
Явных багов не обнаружено; поведение консистентно.  
Потенциальный UX‑нюанс: при нажатии `main_menu` из «старого» сообщения (например, оставшегося в истории после завершения сценария) пользователь может получить новый экран меню, не понимая, к какому именно шагу он сейчас относится (особенно если в чате много старых сообщений с этими же кнопками).

**Рекомендация:**

- Считать это допустимым поведением: кнопка «Главное меню» всегда безопасна.
- В будущем можно добавить короткое пояснение в текст главного меню, что это всегда «актуальное» состояние (например: «Вы вернулись в главное меню. Текущие сценарии сброшены.»).

---

### 2.2. «◀️ Назад»

**Контекст:**  
Кнопка «Назад» реализована как часть навигационной клавиатуры (`add_back` для add‑flow; в edit‑flow — возврат к меню полей).

**Фактическое поведение:**

- В add‑flow:
  - Корректно возвращает к предыдущему полю.
  - В состоянии `ADD_REVIEW` есть отдельные кнопки для редактирования полей и возврата к обзору.
- В edit‑flow:
  - Возврат реализован через меню выбора полей (`EDIT_MENU`) и клавиатуру `get_edit_field_keyboard`.

**Проблема:**  
Явных логических ошибок по кнопке «Назад» не зафиксировано.  
UX‑нюанс: поведение «Назад» не симметрично между add‑flow и edit‑flow (в одном случае — возврат к предыдущему шагу мастера, в другом — возврат к списку полей). Это может немного путать, но в текущем дизайне считается приемлемым.

**Рекомендация:**

- Ничего критического не менять.
- При желании унифицировать тексты подсказок, чтобы пользователю явно проговаривалось, куда именно он возвращается («к предыдущему полю», «к списку полей редактирования»).

---

### 2.3. «❌ Отменить» / `/q`

**Контекст:**  
Используются как явный выход из сценария:

- Кнопка `add_cancel` в add‑flow.
- Кнопка `edit_cancel` в edit‑flow.
- Команда `/q` — fallback во всех `ConversationHandler`.

**Фактическое поведение:**

- Во всех сценариях отрабатывает корректно:
  - Очищаются `user_data_store`, `user_data_store_access_time` и ключи наподобие `editing_lead_id`, `original_lead_data`.
  - Пользователь попадает в главное меню с понятным сообщением («Редактирование отменено» и т.п.).

**Проблема:**  
Не обнаружено.

**Рекомендация:**  
Текущее поведение оставить без изменений.

---

## 3. Add‑flow и работа с документами (2.7)

### 3.1. Прикрепление документа во время add‑flow

**Контекст (сценарий 2.7):**  
Пользователь в процессе добавления лида (add‑flow) нажимает кнопку «Прикрепить» или использует drag‑and‑drop, отправляет **документ‑изображение**, затем продолжает заполнять поля и нажимает «Сохранить».

**Фактическое поведение:**

- Документ обрабатывается `handle_document_during_add`, `photo_file_id` записывается в `user_data_store[user_id]['photo_file_id']`.
- При нажатии «Сохранить» (`add_save_callback`) бот:
  - Берёт `user_data = user_data_store.get(user_id, {})`.
  - Ожидает, что там всё ещё есть `photo_file_id`, чтобы корректно сохранить фото.

**Проблема (из `BOT_SCENARIOS_ANALYSIS.md`):**

- Есть риск, что `photo_file_id` **теряется** между моментом загрузки документа и моментом сохранения:
  - `user_data_store[user_id]` может быть перезаписан или очищен.
  - Данные между `handle_document_during_add` и `add_save_callback` могут не синхронизироваться ожидаемым образом.
- С точки зрения пользователя:
  - Все кнопки отрабатывают (документ принимается, «Сохранить» срабатывает), но **лид может быть сохранён без фото**, что выглядит как «тихая» ошибка.

**Рекомендация (UX‑уровень):**

- Добавить явную проверку перед сохранением:
  - Если пользователь отправлял документ‑изображение, но к моменту `add_save_callback` `photo_file_id` отсутствует — показывать понятное сообщение об ошибке вместо «тихого» сохранения.
- Либо (если фото не критично):
  - После сохранения без `photo_file_id` явно сообщать: «Лид сохранён без прикреплённого изображения (фото не удалось сохранить).»

---

## 4. Неизвестные команды и кнопки

### 4.1. Нажатие старых/неизвестных inline‑кнопок

**Контекст (сценарий 5.2):**  

- Пользователь может нажать кнопку:
  - Из очень старого сообщения, к которому уже не привязан активный сценарий.
  - С `callback_data`, которое больше не поддерживается (например, после обновлений логики).
- Обрабатывается в основном через `unknown_callback_handler`.

**Фактическое поведение (по анализу):**

- `unknown_callback_handler`:
  - Пытается определить, есть ли активный add‑flow (`ADD_REVIEW` и т.п.) и тогда делегировать обработку в `check_add_state_entry_callback`, чтобы корректно добросить callback в `ConversationHandler`.
  - В случае «стейл‑состояния» (есть `_conversation_`‑ключи, но сценарий по сути уже неактивен) очищает состояние через `clear_all_conversation_state` и возвращает пользователя в главное меню.
- Для неизвестных команд есть отдельный `unknown_command_handler`, который:
  - Не перехватывает `/start`, `/q`, `/skip`, `/tag`.
  - В случае действительно неизвестной команды показывает сообщение и возвращает к понятному состоянию.

**Проблема (UX‑нюанс):**

- При нажатии старых кнопок пользователь иногда:
  - Просто возвращается в главное меню без связи с контекстом, который он помнит.
  - Или видит достаточно общее сообщение об ошибке, без пояснения, что «эта кнопка устарела/недействительна».
- С точки зрения UX, это может восприниматься как «бот не реагирует на кнопку как раньше», хотя технически он защищает состояние.

**Рекомендация:**

- В `unknown_callback_handler` использовать более явный текст:
  - Например: «⚠️ Эта кнопка больше неактуальна. Пожалуйста, начните сценарий заново из главного меню.»
- При обнаружении устаревших `_conversation_`‑ключей:
  - Всегда сопровождать очистку состояния понятным сообщением («Предыдущий сценарий был завершён, начнём сначала.»), а не только тихим возвратом в меню.

---

## 5. Активация ConversationHandler при ADD_REVIEW

**Контекст (проблема 3 в `BOT_SCENARIOS_ANALYSIS.md`):**  

- Сценарии:
  - 2.2. Пересылка сообщения → Добавить
  - 2.3. Пересылка во время add‑flow
  - 2.6. Пересылка сообщения с фото во время add‑flow
- В этих кейсах `handle_forwarded_message` может напрямую устанавливать состояние `ADD_REVIEW`.

**Фактическое поведение:**

- Сейчас реализована «обвязка»:
  - `check_add_state_entry_callback` и `unknown_callback_handler` помогают активировать `ConversationHandler`, даже если состояние `ADD_REVIEW` установлено глобальным обработчиком.
  - По анализу, кнопки `add_save`, `add_back`, `add_cancel`, `edit_fullname_from_review`, `add_edit_field_*`, `add_edit_back_to_review` в состоянии `ADD_REVIEW` обрабатываются корректно.

**Проблема (потенциальный edge‑case):**

- В сложных или редких последовательностях действий (например, много раз переслать разные сообщения подряд, прерывать `/q` и снова пересылать) теоретически возможны ситуации, когда:
  - Состояние `ADD_REVIEW` выставлено,
  - но `ConversationHandler` ещё не «подхватил» это состояние,
  - и первая нажатая кнопка может обрабатываться не тем хендлером, который ожидает пользователь.

На практике это не частый кейс, но он может дать впечатление «иногда кнопка на обзоре после пересылки не срабатывает c первого раза».

**Рекомендация:**

- В спорных местах (где используется `handle_forwarded_message` + `ADD_REVIEW`) дополнительно логировать:
  - `current_state`, наличие `_conversation_`‑ключей, `callback_data`.
- При необходимости:
  - Ввести явную отправку «служебного» сообщения, которое гарантированно активирует `ConversationHandler` до показа обзора, чтобы все последующие кнопки были 100% в контексте `ADD_REVIEW`.

---

## 6. Итог

- **Кнопки в основных сценариях** (проверка, добавление, редактирование, теги, пересылка) по текущему анализу работают корректно:  
  реакции консистентны, глобальные команды (`/start`, `/q`) и кнопка главного меню надёжно обрывают сценарии.
- **Основные UX‑нюансы связаны не с тем, что кнопка «не нажимается», а с результатом:**
  - Сценарий 2.7: пользователь думает, что фото сохранится, но из‑за потери `photo_file_id` лид может остаться без изображения.
  - Нажатие старых/неизвестных кнопок может приводить к резкому возврату в меню без явного объяснения, что кнопка устарела.
  - Edge‑кейсы с `ADD_REVIEW` после пересылки сообщений теоретически могут давать редкие ситуации, где первая кнопка работает не так, как ожидается.

Для приоритета:  
1) Критично доработать UX вокруг документов (2.7), чтобы не было «тихого» пропуска фото.  
2) Улучшить сообщения при нажатии старых/неизвестных кнопок.  
3) Продолжать мониторинг логов по активации `ConversationHandler` на `ADD_REVIEW` и, при необходимости, добавить ещё одну защиту.  